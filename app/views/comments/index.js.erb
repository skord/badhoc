<% unless @comments.empty? %>
var a = "<%= escape_javascript(raw render :partial => 'comments/comments' )%>";
$(a).prependTo("#comments").hide().each(function (i){
	var self = $(this);
	window.setTimeout(function() { self.fadeIn('slow'); },1000*i);
});
$('#post_comments_count').text($('#comments .comment').length + ' comments');
<% if current_admin %>
	$(".admin").show();
<% end %>
<% end %>
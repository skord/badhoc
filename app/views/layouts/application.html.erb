<!DOCTYPE html>
<html>
	<head>
	  <title>Badhoc</title>
		<%= stylesheet_link_tag  'formtastic', 'pagination', 'post', 'comment', 'blueprint/screen', 'site', :cache => true %>
	  <%= javascript_include_tag :defaults %>
	  <%= csrf_meta_tag %>

	</head>
	<body>
		<div class='container'>
			<%= render :partial => 'shared/thumbbar'%>
			<div class='span-24 last'>
					<div id='notice'><%= notice %></div>
			  	<div id="alert"><%= alert %></div>
			</div>

			<%= render :partial => 'shared/admin_box'%>
	
			<div class='content'>
				<%= yield %>	
			</div>
	
		</div>
	</body>
</html>
